function solution(num1, num2) {
    if (num1 === num2) {
        return 1;
    } else {
        return -1;
    }
}

// function solution(num1, num2) {
//     var answer = num1 === num2 ? 1 : -1;
//     return answer;
// }

// 삼항연산자를 사용 num1===num2가 true면 1을 반환 false면 -1 반환
// 내 코드보다 보기 좋은 코드 